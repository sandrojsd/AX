<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
             Padding="20"
             x:Class="InstaXamarinMobile.Views.AlterarDados">

    <ScrollView BackgroundColor="Red" VerticalOptions="Center" HeightRequest="350">
        <StackLayout Padding="10" >
            <Label Text="Alterar Senha" FontSize="30"/>

            <Entry Placeholder="Nome"  Text="{Binding Nome}"/>

            <Entry Placeholder="E-mail" Text="{Binding Email}" />

            <Entry IsPassword="True" Placeholder="Senha" Text="{Binding Senha}" />

            <Button x:Name="btnAlterar" Text="Atualizar Dados" BackgroundColor="Red"/>
            <Button x:Name="btnFechar" Text="Cancelar"/>

        </StackLayout>
    </ScrollView>


</pages:PopupPage>