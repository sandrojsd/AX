<?xml version="1.0" encoding="utf-8" ?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms"
              xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              x:Class="InstaXamarinMobile.LOADING"
              BackgroundColor="Transparent"
			        x:Name="LoadBase">

  <Grid VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" BackgroundColor="Transparent">

    <!--	FUNDO	-->
    <StackLayout Style="{StaticResource Loading}"/>

    <!--	LOADING + TEXTO	-->
    <StackLayout VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" BackgroundColor="Transparent">

      <ActivityIndicator x:Name="Loading" VerticalOptions="EndAndExpand" HorizontalOptions="Center" Color="Blue" IsRunning="True" IsVisible="True" />
      <Label x:Name="LoadingTexto" Text="Carregando.." VerticalOptions="StartAndExpand"  HorizontalOptions="Center" TextColor="Black" />

    </StackLayout>

  </Grid>
</StackLayout>